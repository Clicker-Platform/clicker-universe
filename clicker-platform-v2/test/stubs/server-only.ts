// Test stub for the `server-only` package. In production `server-only` throws at
// build time if imported into a client bundle; under vitest there is no such
// constraint, so this is an empty module that simply resolves.
export {};
